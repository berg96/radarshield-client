gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java

# Remember to never publicly share your keystore.
# See https://flutter.dev/docs/deployment/android#reference-the-keystore-from-the-app
key.properties
**/*.keystore
**/*.jks
# Exception: the stable DEBUG/sideload key is intentionally committed so every CI
# build is signed with the same cert (in-place updates). It is a throwaway debug
# key (pass 'android'), NOT a Play release key. A real release keystore must NOT
# be committed — it goes through local.properties / CI secrets instead.
!app/radarshield-debug.keystore
